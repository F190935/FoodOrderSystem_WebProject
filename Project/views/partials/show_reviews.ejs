
<% for(var i = 0; i < prod.length; i++) { %>
    <tr class="data-color">
        <td><%= i + 1 %></td>
        <td><%= prod[i].food_reviews %></td>
        <td><%= prod[i].food_rating %></td>
    </tr>
    <% } %>
